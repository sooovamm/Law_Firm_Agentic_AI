# Legal CMS - Backend Python dependencies
# Install with:  pip install -r requirements.txt
# Requires Python >= 3.11
#
# Note: the frontend is a Next.js app - its dependencies live in
# frontend/package.json and install with `npm install`, not pip.

# --- Web framework & server ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12        # multipart/form-data (file uploads)

# --- Database & migrations ---
sqlalchemy>=2.0.35
alembic>=1.13.3
psycopg2-binary>=2.9.10         # PostgreSQL driver

# --- Validation & settings ---
pydantic>=2.9.2
pydantic-settings>=2.5.2
email-validator>=2.2.0

# --- Auth & security ---
python-jose[cryptography]>=3.3.0  # JWT encode/decode
bcrypt>=4.2.0                      # password hashing

# --- AI / agents ---
langgraph>=1.0.0                # intake, email, and deadline agent graphs
openai>=1.54.0                  # LLM client (needs OPENAI_API_KEY at runtime)

# --- Document processing ---
pypdf>=5.1.0                    # PDF text extraction
python-docx>=1.1.2              # DOCX text extraction
pillow>=11.0.0                  # image handling

# --- Storage ---
boto3>=1.35.0                   # S3 storage backend (local storage needs no extra deps)