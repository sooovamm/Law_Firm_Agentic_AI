# Legal CMS - Backend development & test dependencies
# Install with:  pip install -r requirements-dev.txt
# (installs the runtime deps too)

-r requirements.txt

pytest>=8.3.3
pytest-asyncio>=0.24.0
httpx>=0.27.2                   # TestClient / async HTTP in tests
ruff>=0.6.9                     # linter / formatter
